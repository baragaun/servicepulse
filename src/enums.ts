export enum ServiceHealth {
  unknown = 'unknown',
  ok = 'ok',
  limited = 'limited',
  offline = 'offline',
  unreachable = 'unreachable',
  failedToParse = 'failedToParse',
}

export enum ServiceType {
  bgdata = 'bgdata',
}
